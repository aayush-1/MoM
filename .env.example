TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
GOOGLE_FOLDER_ID=your-google-drive-folder-id-here

# For server deployment: paste the entire contents of token.json here.
# This avoids needing to copy token.json to the server.
# Leave empty when running locally (token.json file is used instead).
GOOGLE_TOKEN_JSON=
